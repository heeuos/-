module logic_gate5(
    input  a, b,
    output and_out, or_out, xor_out, nor_out, nand_out
);
    assign and_out  = a & b;
    assign or_out   = a | b;
    assign xor_out  = a ^ b;
    assign nor_out  = ~(a | b);
    assign nand_out = ~(a & b);
endmodule
